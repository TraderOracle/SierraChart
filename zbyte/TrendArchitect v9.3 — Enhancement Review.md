# TrendArchitect v9.3 — Enhancement Review (2026-07-03)

Full proposal/verdict matrix from the 10-agent review workflow (5 specialist
reviewers + 5 adversarial verifiers; ~1.02M tokens). Every proposal was
fact-checked against the Sierra Chart docs and the v2760 ACS_Source headers.
Implemented set + final input map: see directives/trendarchitect.md §v9.3.
Verdicts: CONFIRMED = load-bearing facts verified; PLAUSIBLE = sound but
unverifiable detail (V4 shipped WITH the verifier's snapshot-ordering fix);
REFUTED = not built (P3 — wrong time API).

====================================================================================================
REVIEWER: prism-gates

  [E1] Bar-Close Confirmation for PRISM signals and alerts (opt-in)
      priority=1  verdict=CONFIRMED
      mechanism: New YesNo input IN_PRISM_CONFIRM_CLOSE = 106 ('PRISM: Confirm Signals on Bar Close'), default No (parity). In TrendArchitect.cpp: (a) signal-write block (~lines 871-879): when enabled AND idx == sc.ArraySize-1 AND sc.GetBarHasClosedStatus(idx) != BHCS_BAR_HAS_CLOSED, write 0.0f to SG_PRISM_BULL/SG_PRISM_BEAR/SG_PRISM_MQ_BULL/SG_PRISM_MQ_BEAR instead of the arrow values — the live bar shows no arrow until it closes; historical bars keep their last-tick (final) values automatically. (b) alerts block (~967-1012): skip alerts 1-7 for the live bar; instead capture bool new_bar_started in the commit
      inputs: 
      risk: Entries confirmed one bar later (intended trade-off). Must not suppress on historical replay/full recalc where every bar reads as closed — the existing !sc.IsFullRecalculation guard already covers the alert side. Off by default preserves TradingView-parity repaint behavior.
      verify-notes: All load-bearing facts verified. APIs: docs confirm `int GetBarHasClosedStatus(int BarIndex)` and no-arg auto-loop overload, returning BHCS_BAR_HAS_CLOSED (any bar except last), BHCS_BAR_HAS_NOT_CLOSED (always the last bar), BHCS_SET_DEFAULTS; explicitly index-based and designed for Number-of-Trades (tick) bars. `int SetAlert(int AlertNumber)` auto-loop overload confirmed; SCDateTime::GetAsDouble already used at TrendArchitect.cpp:968. Hook points confirmed: signal writes at TrendArchitect.cpp:876-879, alerts block 963-1012 (guard at 967 already has !sc.IsFullRecalculation && !sc.DownloadingHi

  [E2] Strict Gate Mode: close the chop AND-hole, re-validate delayed releases, require 2 elevation criteria (opt-in)
      priority=1  verdict=CONFIRMED
      mechanism: New YesNo input IN_PRISM_GATE_STRICT = 107, default No; pass as bool strict into compute_PRISMSignals. Three boolean edits in mod_PRISMSignals.h: (a) chop OR-mode (lines 290-293): strict ? (chop_bull = mq_enable && er_valid && fk_valid && ((er < er_thresh) || (fk_norm < kama_slope_min))) : existing AND — removes the rescue where a +0.03-ATR fast-KAMA micro-swing overrides ER=0.05 chop; symmetric for bear. (b) release re-validation (lines 210-216 SL, 233-239 BQ): in strict mode bull_released additionally requires (d1==-1 && d2==-1) (rails still agree bull on the release bar) and bq_bull_ok; bq_
      inputs: 
      risk: Strict chop-OR downgrades some legitimate early-trend signals (KAMA lags at trend starts) — they remain visible as MQ dots so nothing is hidden; user can leave baseline mode on. Elevation quorum makes counter-trend elevations (e1 pairs only with itself) nearly impossible — acceptable for this trader
      verify-notes: Pure boolean edit, no new state, rollback-safe by construction. All four hook line ranges verified exactly in mod_PRISMSignals.h: SL release logic at 210-216 (bull_released/bear_released), BQ release at 233-239, chop AND at 290-293, elevation OR at 355-356; compute_PRISMSignals call site at TrendArchitect.cpp:846-862. Scope claims verified: d1/d2 declared at line 148 and current-bar values live at both release sites; bq_bull_ok/bq_bear_ok defined at 185-186, before both. The AND-hole analysis is correct (chop_bull requires er<thresh AND fk_norm<min; a transient +0.03-ATR fkama slope defeats it

  [E3] Opposite-signal cooldown (whipsaw guard, opt-in)
      priority=2  verdict=CONFIRMED
      mechanism: New int input IN_PRISM_FLIP_GUARD = 108 ('PRISM: Opposite-Signal Cooldown (bars, 0=off)'), default 0 (parity), suggested 5-8. Add to TARecurrence (TA_State.h, PRISM block ~line 91): int last_full_bar = -1000000; int last_full_dir = 0. In mod_PRISMSignals.h AFTER the final signal computation (after lines 358-362 so elevation cannot bypass it): if (guard > 0 && out.full_bull && st.last_full_dir == -1... i.e. previous full signal was BEAR ... && (idx - st.last_full_bar) <= guard) { out.full_bull = false; out.mq_bull = true; } symmetric for bear. Then update state: if (out.full_bull) { st.last_ful
      inputs: 
      risk: Trivial complexity; only behavioral risk is demoting the first leg of a fast reversal. Interacts predictably with E2 (both demote to MQ, never drop). Must be applied post-elevation or elevation would re-promote around it.
      verify-notes: Structurally sound and rollback-safe. TARecurrence PRISM block confirmed at TA_State.h:91-101; new int fields with default initializers (last_full_bar=-1000000, last_full_dir=0) inherit commit/rollback (TrendArchitect.cpp:526-547) and the idx==0 full-recalc reset (528-529), so bar-index-based state is deterministic across recalcs. Final-signal computation confirmed at mod_PRISMSignals.h:358-362; applying the guard after line 362 correctly prevents elevation from bypassing it, and updating last_full_bar/dir only from the post-guard out.full_* keeps the state coherent (a demoted counter-flip doe

  [E4] Real bid/ask delta as CVD source (opt-in), feeding elevation c4 and delta-strength displays
      priority=2  verdict=CONFIRMED
      mechanism: New dropdown input IN_CVD_SOURCE = 109 ('Delta Source: Geometric Proxy | Real Bid/Ask Volume'), default Geometric (parity). In mod_Candles.h CVD block (lines 218-232): when Real mode and the bar has data (sc.BaseData[SC_ASKVOL][idx] + sc.BaseData[SC_BIDVOL][idx] > 0): cvd_up = sc.BaseData[SC_ASKVOL][idx]; cvd_dn = sc.BaseData[SC_BIDVOL][idx]; out.cvd_net = cvd_up - cvd_dn; cvd_agg = |cvd_net| / max(sc.BaseData[SC_VOLUME][idx], 1) * 100; else silently fall back to the geometric proxy for that bar (backfilled data without bid/ask splits). Everything downstream is untouched: SG_CVD_STORE percent-
      inputs: 
      risk: Historical segments lacking bid/ask splits mix proxy and real values inside one 50-bar rank window at the boundary (transient, self-heals). Percentile distribution shifts vs proxy — acceptable because the threshold is a rank, not an absolute. Off by default keeps TV parity.
      verify-notes: APIs verified in the ACSIL Variables/Arrays docs: SC_ASKVOL ('volumes of the trades that occurred at the Ask price or higher') and SC_BIDVOL ('...at the Bid price or lower') are documented sc.BaseData[][] constants, with sc.AskVolume[] / sc.BidVolume[] shortcut references; SC_VOLUME likewise (code already uses sc.Volume at mod_Candles.h:58). Hook points verified: geometric cvd_up/cvd_dn at mod_Candles.h:226-227, cvd_net 228, cvd_agg 229, SG_CVD_STORE write 232, rank pipeline 234-249 (untouched), rank-history Arrays[0] write 249; e4 consumers at mod_PRISMSignals.h:352-353 read only cvd_rank/cvd

  [E5] Auto-Optimizer adverse-excursion (stop-aware) scoring (opt-in)
      priority=3  verdict=CONFIRMED
      mechanism: New float input IN_AO_STOP_ATR = 110 ('AO: Stop-Out ATR (0 = off)'), default 0.0 (parity), suggested 1.0. Add double worst = 0.0 to AOPendingState (TA_State.h:52-58). In mod_AutoOptimizer.h process_pending (lines 176-204), alongside the favorable-excursion update compute adverse = (p.dir == 1) ? (p.price - sc.Low[idx]) : (sc.High[idx] - p.price); track p.worst = max(p.worst, adverse); when stop_atr > 0 and p.worst >= p.atr * stop_atr and p.best < 1, finalize immediately: scores.push_back(0), trim deque, p.price = 0 (stopped out before reaching Tier 1 = scored as a loss). Optionally freeze p.be
      inputs: 
      risk: When enabled, ao_eff_len diverges from the TV baseline (that is the point; 0.0 default preserves parity byte-for-byte since the new branch never runs). Score distributions shift lower in whipsaw regimes, increasing the all-zero-weights warm-up fallback to the medium length — which is a safe fallback
      verify-notes: Hook points verified: AOPendingState at TA_State.h:52-58; process_pending lambda at mod_AutoOptimizer.h:176-204 with the favorable-excursion tier chain at 184-187, expiry/scoring at 189-195, arming at 197-203; ao_pend[3] lives inside TARecurrence (TA_State.h:107) so a new field inherits commit/rollback and full-recalc reset automatically; deque<int> values stay 0-3 so f_convex_weight_blend / f_ao_score_weight are untouched; compute_AutoOptimizer signature + call site TrendArchitect.cpp:829-839 confirmed; sc.High/sc.Low already used for MFE at line 184. MFE-only blind-spot analysis is accurate 
====================================================================================================
REVIEWER: order-flow

  [OF-1] True bid/ask delta drop-in for the CVD proxy (same downstream units)
      priority=1  verdict=CONFIRMED
      mechanism: New input IN_OF_TRUE_DELTA=106 ('OF: Use True Bid/Ask Delta', Yes/No, default No). In sc.SetDefaults add sc.MaintainAdditionalChartDataArrays = 1 (unconditional - required for the arrays to populate, harmless otherwise). Chart-level presence detection: in the existing idx==0 runtime block of TrendArchitect.cpp, scan the last min(sc.ArraySize,500) bars: present = (bars where sc.AskVolume[i]+sc.BidVolume[i] > 0) >= 50% of bars with sc.Volume[i] > 0; store in a new plain TAState field `bool of_data_present` (TA_State.h, OUTSIDE TARecurrence - it is not per-bar recurrence and must not roll back; r
      inputs: IN_OF_TRUE_DELTA=106 'OF: Use True Bid/Ask Delta (CVD)' Yes/No, default No.
      risk: LOW-MEDIUM. (1) Users must have 1-Tick intraday storage and may need to reload the chart (Edit >> Reload and Recalculate) after the DLL update for Sierra to rebuild the additional arrays from the .scid file - document in the input description. (2) Old historical segments without bid/ask data mix pro
      verify-notes: All load-bearing facts verified. APIs: sc.MaintainAdditionalChartDataArrays exists (set in sc.SetDefaults); sc.AskVolume/sc.BidVolume = sc.BaseData[SC_ASKVOL]/[SC_BIDVOL] with verbatim-matching semantics ('at the Ask price or higher'/'at the Bid price or lower'), 1-Tick Intraday Data Storage required, arrays read 0 where data absent; SC_UPVOL/SC_DOWNVOL tier-2 semantics match; subgraph extra arrays are exactly 12 (Arrays[0..11]), auto-sized to Data. Source: input 106 free (highest used = 105, TA_Inputs.h); SG_CVD_STORE(=42) uses only Data+Arrays[0] today so Arrays[1..3] are free (verified by g

  [OF-2] Delta-direction agreement gate: downgrade full arrows lacking signal-side aggression
      priority=2  verdict=CONFIRMED
      mechanism: Inputs: IN_OF_AGREE_GATE=107 ('OF Gate: Delta Agreement', Yes/No, default No), IN_OF_AGREE_BARS=108 (int 1-5, default 2, 'OF Gate: Agreement Window (bars)'). Implement as a post-processing block in TrendArchitect.cpp inserted between the compute_PRISMSignals call (line 862) and the signal-size/subgraph-write block (line 864) - this single hook point automatically propagates to the arrow subgraphs (lines 876-879) AND the alert block (lines 998-1005) because both read the mutated PRISMResult. Code: bool of_ok = tas.of_data_present && sc.Subgraph[SG_CVD_STORE].Arrays[2][idx] > 0.5f; if (of_gate_e
      inputs: IN_OF_AGREE_GATE=107 Yes/No default No; IN_OF_AGREE_BARS=108 int 1-5 default 2.
      risk: LOW. Intrabar flicker: live-bar delta oscillates, so an arrow can appear/disappear until bar close - identical to the study's existing repaint model (documented behavior). Trade-off: strong reversal entries occasionally begin with lagging delta; the 2-bar window (vs signal-bar-only) softens this, an
      verify-notes: Hook point exists exactly as claimed: compute_PRISMSignals call ends at TrendArchitect.cpp:862, next block starts 864-865; arrow subgraph writes at 876-879 and alert conditions at 998-1005 are — verified by grep of all prism.* usages — the ONLY consumers of full_bull/full_bear/mq_bull/mq_bear (prism.cur_dir at 907 and prism.er at 882/930 are unaffected). PRISMResult is a plain by-value struct (mod_PRISMSignals.h:45-56); module-internal recurrence (prism_last_sig, structure-lock/BQ holds) is keyed to RAW rail signals, not the output flags, so post-hoc downgrade cannot desync state. Downgrade ro

  [OF-3] Cumulative-delta divergence veto (price up, real CVD down)
      priority=3  verdict=CONFIRMED
      mechanism: Inputs: IN_OF_DIV_VETO=109 ('OF Gate: CVD Divergence Veto', Yes/No, default No), IN_OF_DIV_LOOKBACK=110 (int 10-50, default 20). Windowed cumulative delta avoids float32 precision loss of a running total: in the OF-1 code path of mod_Candles.h (or in the 7C block), compute cvd_L = sum of SG_CVD_STORE.Arrays[1][idx-i] for i in 0..L-1 (guarded i<=idx) and store history in SG_CVD_STORE.Arrays[3][idx] = (float)cvd_L. This equals the change in session cumulative delta over L bars, computable per closed bar with no state. In the 7C gate block (after OF-2's check): bool div_bull = (cvd_L < 0.0) && (i
      inputs: IN_OF_DIV_VETO=109 Yes/No default No; IN_OF_DIV_LOOKBACK=110 int 10-50 default 20.
      risk: MEDIUM. Divergence length is regime-dependent: L=20 on a 500-tick NQ chart spans minutes in RTH but much longer overnight; a fixed L can veto valid breakout longs that start from a short-covering base (negative CVD backdrop is common at V-bottoms). Mitigations: counter-trend reversal arrows usually 
      verify-notes: Load-bearing facts verified: inputs 109-110 free; SG_CVD_STORE.Arrays[3] free (only Data+Arrays[0] used at baseline, [1..2] claimed by OF-1); 12 extra arrays per subgraph confirmed against docs; hook point (7C block between TrendArchitect.cpp:862-864) exists; windowed L-bar re-summation is stateless/tick-idempotent so commit/rollback untouched; Arrays[3][idx-3] history is valid provided the write is unconditional per bar as the proposal specifies (7C sits on an unconditional code path). The suggested mitigation is accurate: ct_bull/ct_bear exist as locals at mod_PRISMSignals.h:304-305 and expo

  [OF-4] Intra-bar absorption veto via SC_ASKBID_DIFF_HIGH/LOW delta retracement
      priority=4  verdict=CONFIRMED
      mechanism: Inputs: IN_OF_ABSORB_VETO=111 ('OF Gate: Absorption Veto', Yes/No, default No), IN_OF_ABSORB_RETRACE=112 (float 0.30-0.90, default 0.60). Uses the only per-bar arrays that carry INTRA-bar delta-path information: dmax = sc.BaseData[SC_ASKBID_DIFF_HIGH][idx] (maximum of the bar's running askvol-bidvol) and dmin = sc.BaseData[SC_ASKBID_DIFF_LOW][idx] (minimum), with dclose = askv-bidv from OF-1. Absorption signature for a bull signal bar: buyers were aggressive intrabar (dmax meaningfully > 0) but the delta collapsed off its high into the close - passive sellers absorbed the buying: bull_absorbed
      inputs: IN_OF_ABSORB_VETO=111 Yes/No default No; IN_OF_ABSORB_RETRACE=112 float 0.30-0.90 default 0.60.
      risk: MEDIUM. (1) Live-bar semantics: dmax/dmin grow intrabar, so the veto can only strengthen as the bar forms - an arrow may vanish late in the bar; consistent with the study's repaint model but the most flicker-prone of the four proposals. (2) The 0.60 retrace default is a starting point, not validated
      verify-notes: The critical API claim verified against the ACSIL members docs: SC_ASKBID_DIFF_HIGH = 'maximum difference between the Ask volume and the Bid volume for the bar', SC_ASKBID_DIFF_LOW = minimum — exact constant names and semantics match; both require sc.MaintainAdditionalChartDataArrays=1 (provided by OF-1) plus 1-Tick Intraday Data Storage, and read 0 for historical data lacking Ask/Bid volume, so the significance-floor comparisons are self-disabling exactly as claimed. The claimed companion SC_ASK_BID_VOL_DIFF_MOST_RECENT_CHANGE also exists per the docs. Source: inputs 111-112 free; hook point 
====================================================================================================
REVIEWER: regime-chop

  [REGIME-1] Effective seconds-per-bar from bar timestamps (fixes PRISM Adaptive on tick/volume/range charts)
      priority=5  verdict=CONFIRMED
      mechanism: (1) TA_State.h: add 'double est_sec_per_bar = 0.0;' to TAState (NOT TARecurrence — it is a per-full-recalc constant like Pine's timeframe.in_seconds, not rollback state; place near 'int last_index' at line ~120). (2) TrendArchitect.cpp, inside a new 'if (idx == 0)' block immediately above section 7 (line ~787): call sc.GetBarPeriodParameters(bp) with n_ACSIL::s_BarPeriod bp. If bp.ChartDataType == INTRADAY_DATA && bp.IntradayChartBarPeriodType == IBPT_DAYS_MINS_SECS, set est = (double)bp.IntradayChartBarPeriodParameter1 (authoritative; identical to today's behavior on time charts). Otherwise (
      inputs: IN_ADAPT_TICK_EST = 106, 'PRISM Adaptive: Estimate Bar Seconds (tick/volume/range)', Yes/No, default No.
      risk: Median over the most recent 300 bars is dominated by the latest session; a chart reloaded overnight briefly over-estimates seconds/bar → tf_ratio conservative (too little scaling), never dangerous. Estimate is frozen between full recalcs, so intraday pace shifts (RTH open vs lunch) are not tracked —
      verify-notes: All load-bearing facts verified. Docs (fetched 2026-07-03): sc.SecondsPerBar deprecation wording quoted verbatim ('out of date/deprecated. Instead use the sc.GetBarPeriodParameters and sc.SetBarPeriodParameters functions'); signature 'int GetBarPeriodParameters(n_ACSIL::s_BarPeriod&)' exact; s_BarPeriod members ChartDataType/IntradayChartBarPeriodType/IntradayChartBarPeriodParameter1 exist and Parameter1 = SECONDS for IBPT_DAYS_MINS_SECS (doc-confirmed); sc.BaseDateTimeIn 'is the starting time of that bar' (doc-confirmed); SCDateTime GetAsDouble is in DAYS (doc-confirmed). Hook points exact: T

  [REGIME-2] Range Regime Gate — non-directional 2-of-3 vote block (Choppiness Index + ADX floor + ATR percentile) feeding the existing downgrade path
      priority=4  verdict=CONFIRMED
      mechanism: New module /root/code/trendarchitect/source/modules/mod_RangeRegimeGate.h returning {bool range_active; int votes; double chop, adx, atr_pctl;}. Vote 1 — Choppiness Index: chop = 100*log10(sumTR(chop_len)/(HH(chop_len)-LL(chop_len)))/log10(chop_len) using ta_true_range(sc,j) over the window (O(len) per bar, len=14 default; guard HH==LL → chop=100); vote if chop > 61.8. Vote 2 — Wilder ADX(adx_len=14): +DM/-DM/TR Wilder RMAs and RMA of DX, all as four new doubles in TARecurrence (rrg_dmp, rrg_dmn, rrg_tr, rrg_adx) mirroring the ta_wilder_atr_step warm-up pattern so they participate in the commi
      inputs: IN_RRG_ENABLE=107 (No), IN_RRG_CHOP_LEN=108 (14), IN_RRG_CHOP_THRESH=109 (61.8), IN_RRG_ADX_LEN=110 (14), IN_RRG_ADX_FLOOR=111 (20.0), IN_RRG_ATRP_LOOKBACK=112 (200), IN_RRG_ATRP_FLOOR=113 (25.0), IN_
      risk: Range-exit lag: ADX needs several bars to rise after a breakout, so the FIRST breakout signal can be downgraded to a dot (mitigated by: 2-of-3 lets fast-dropping CHOP + rising ATR-pct outvote stale ADX; elevation c2/c4 rescue; and this trader explicitly prefers missing an entry over taking a chop lo
      verify-notes: Hook points exact: TRG call ends TrendArchitect.cpp:750 with atr14 already computed (:550); suppress args passed at :854 as plain bools ('trg.suppress_buy, trg.suppress_sell,') so ORing rrg.range_active needs zero module changes; downgrade path is exactly mod_PRISMSignals.h:296-297 (is_mq_* = would_* && (chop_* || suppress_*)) and elevation c1-c4 spans exactly :302-356 with CVD rank>=95 rescue at :352-353 — never-drop preserved as claimed. Subgraph 50 free (highest used 49; TA_Subgraphs.h:112-114); bumping SG_LAST_HIDDEN_STORE to 50 is sufficient because the idx==0 enforcement loops (cpp:633-6

  [REGIME-3] Calibrated TRG mode: Hurst threshold 0.72 + accel-vote deadband (fixes the degenerate vote and the sign-flap)
      priority=3  verdict=CONFIRMED
      mechanism: One opt-in bool restores the intended 2-of-3 semantics without touching parity defaults. mod_TrendRegimeGate.h signature gains (bool cal_mode, double hurst_cal_thresh, double accel_deadband); voting block (lines 80-91) becomes: h_thr = cal_mode ? hurst_cal_thresh : hurst_thresh; if (out.hurst > h_thr) { bull_v++; bear_v++; } ... db = cal_mode ? accel_deadband : 0.0; if (r_accel_ema > db && tc.tc_base > tc_base_prev) bull_v++; if (r_accel_ema < -db && tc.tc_base < tc_base_prev) bear_v++. TrendArchitect.cpp passes the three new args at the call (lines 744-750). Defaults from the simulation evide
      inputs: IN_TRG_CAL_ENABLE=118 'Regime Gate: Calibrated Hurst/Accel' (No), IN_TRG_HURST_CAL_THRESH=119 (0.72, limits 0.5-0.95), IN_TRG_ACCEL_DEADBAND=120 (0.005, limits 0.0-0.05).
      risk: Direction of net P/L effect is not guaranteed: today's degenerate Hurst inflates BOTH vote counts, so calibration makes suppression rarer overall — in weak trends some counter-trend losers that the accidental 1-of-2 behavior caught will now pass TRG (RRG usually catches them instead, as weak trends 
      verify-notes: Hook points exact: mod_TrendRegimeGate.h signature is :26-37 and the voting block is :80-91, with line 84 literally 'if (out.hurst > hurst_thresh) { bull_v++; bear_v++; }  // hurst just adds to both' and accel votes at :85-86 gated on tc_base direction as described; eff_* block at cpp:582-586 (Simple forces 0.55, Advanced default input 0.50 at cpp:401-402); call site :744-750; f_hurst_rs at TA_Algorithms.h:99-121 exactly; TARecurrence hurst/accel_ema at TA_State.h:84-86 exactly; inputs 118-120 free; IN_TRG_HURST_THRESH=72 confirmed. Degeneracy claim INDEPENDENTLY REPLICATED: exact Python port 

  [REGIME-4] Self-calibrating ER gate: percentile-rank tightening of the existing Quality-Gate chop test
      priority=2  verdict=CONFIRMED
      mechanism: The chop test (mod_PRISMSignals.h:290-293) currently uses the fixed er_thresh=0.20. Add (bool er_adapt, int er_adapt_lb=250, float er_adapt_pct=40) to compute_PRISMSignals; after er is computed (line 263), when enabled and idx > PRISM_ER_LEN+10: avail = min(er_adapt_lb, idx-1); below = count of SG_RAW_ER[idx-i] < er for i=1..avail; er_rank = 100*below/avail. Then replace the 'er < er_thresh' term in chop_bull/chop_bear with 'er < er_thresh || er_rank < er_adapt_pct' — strictly tightening (OR only adds chop detections, never removes the absolute floor), so with the input off the code path is by
      inputs: IN_ER_ADAPT_ENABLE=115 'Quality Gate: Adaptive ER Percentile' (No), IN_ER_ADAPT_LOOKBACK=116 (250, limits 50-1000), IN_ER_ADAPT_PCT=117 (40.0, limits 5-95).
      risk: On strong trend days the trader wants to keep, a relative ER dip during a healthy pullback can rank <40% and downgrade a valid continuation entry (the fkama-slope AND-term usually saves these: slope stays above 0.03 in real trends, and chop requires BOTH). O(250) scan per call on the live bar — negl
      verify-notes: Hook points exact: compute_PRISMSignals signature spans mod_PRISMSignals.h:74-99; er computed :254-262 with out.er at :263; chop terms at :290-293 are exactly 'er < (double)er_thresh && fk_norm ...' so the OR-substitution is strictly tightening (chop still requires the fk_norm AND-term, preserving the healthy-trend rescue described); downgrade never drops (is_mq_* at :296-297, elevation :302-356). Call site cpp:846-862 (er_thresh at :855), SetDefaults anchor IN_PRISM_ER_THRESH at cpp:433-434, inputs 115-117 free, SG_RAW_ER=27 (TA_Subgraphs.h:77). Tick-safety verified: SG_RAW_ER[idx] is written
====================================================================================================
REVIEWER: repaint-confirm

  [P1] Signal Confirmation Mode input — Live (TV parity) | Bar Close (confirmed markers drawn retroactively at idx-1)
      priority=1  verdict=CONFIRMED
      mechanism: 1) TA_Inputs.h: add `#define IN_SIG_CONFIRM_MODE 106`. TrendArchitect.cpp SetDefaults (after IN_FC_SHOW_TC, ~line 501): Name="PRISM Signal Confirmation", SetCustomInputStrings("Live (TradingView parity);Bar Close (confirmed)"), SetCustomInputIndex(0) — default Live keeps the parity baseline byte-identical. 2) State block (TrendArchitect.cpp:526-547): add `bool new_bar_commit=false;` set true inside the `idx > tas.last_index` branch (this transition — the first call at index N+1 — IS the canonical ACSIL closed-bar detector for bar N with AutoLoop=1; verified: sc.GetBarHasClosedStatus() only ret
      inputs: IN_SIG_CONFIRM_MODE (index 106): "PRISM Signal Confirmation" = Live (TradingView parity) | Bar Close (confirmed); default Live.
      risk: Low. Additive and opt-in; Live mode remains bit-identical. Inherent one-bar confirmation latency (one trade on tick bars; on thin 30s-5m time bars the confirming tick can arrive late — see P3). Marker price assumed nonzero (always true for NQ/ES/GC price levels).
      verify-notes: All load-bearing facts verified. APIs: sc.Subgraph[].Arrays has exactly 12 extra persistent float arrays (Arrays[0..11], same size as Data) per ACSIL_Members_scSubgraph ('As of 2016-02 there are 12 extra arrays'); sc.EarliestUpdateSubgraphDataArrayIndex exists with exactly the claimed semantics ('no need to set... if a study does not make changes... at an index earlier than... the earliest sc.Index set for a study during an update'; 'should not be set when sc.IsFullRecalculation is TRUE/1'); sc.GetBarHasClosedStatus returns BHCS_BAR_HAS_CLOSED 'always... for any bar other than the last bar' an

  [P2] Bar Close alert semantics — fire alerts only for committed bar-N state, latched on the closed bar's timestamp
      priority=1  verdict=CONFIRMED
      mechanism: Extend the alerts block (TrendArchitect.cpp:963-1012) with a mode branch. Live mode: existing block unchanged. Bar Close mode: fire inside `if (new_bar_commit && idx>0 && !stale_pass && !sc.IsFullRecalculation && !sc.DownloadingHistoricalData)` — drop the `idx == sc.ArraySize-1` gate in this mode: the commit branch runs exactly once per genuinely new bar in real time (historical sweeps are excluded by IsFullRecalculation/DownloadingHistoricalData), and this also fixes the existing missed-alert edge where two bars form in one chart update and the older bar commits at idx==ArraySize-2, which the
      inputs: None beyond P1's IN_SIG_CONFIRM_MODE — alert behavior follows the same mode switch so markers and alerts can never disagree.
      risk: Low. Opt-in; only ordering risk is double accounting if P3's clock-confirm also fires — the bar_time latch already dedupes that. Alert arrives one bar later than Live mode by design.
      verify-notes: Verified. Alerts block is exactly TrendArchitect.cpp:963-1012 with the fire_once/alert_fired_time[16] timestamp latch (TA_State.h:122-126, sized 16 > alert numbers 1-13; GetAsDouble already used at line 968). sc.SetAlert overloads confirmed in docs: 'int SetAlert(int AlertNumber); Auto-looping only' and 'int SetAlert(int AlertNumber, SCString Message); Auto-looping only' — both apply (sc.AutoLoop=1), so the '(confirmed)' message suffix works. sc.AlertOnlyOncePerBar exists as claimed ('can be set to 1 or 0... alert given only once per bar'); not using it is reasonable. sc.IsFullRecalculation ve

  [P3] Time-bar clock confirmation (optional): confirm bar N by wall clock on 30s-5m charts without waiting for the next trade
      priority=3  verdict=REFUTED
      mechanism: Sub-toggle IN_SIG_CONFIRM_CLOCK (index 107, YesNo, default No), active only when Bar Close mode is on AND the chart is time-per-bar (guard via sc.GetBarPeriodParameters → IntradayChartBarPeriodType == IBPT_DAYS_MINS_SECS; skip tick/volume/range bars, which have no clock close). When enabled set sc.UpdateAlways=1 (idx==0 runtime block) so the study is called on the chart update interval even without trades. On calls where idx==sc.ArraySize-1 and no new bar has formed: compute bar-end = sc.BaseDateTimeIn[idx] + SecondsPerBar (per the ACSILProgrammingConcepts-prescribed method: BaseDateTimeIn + G
      inputs: IN_SIG_CONFIRM_CLOCK (index 107): "Bar Close: Clock-Confirm Time Bars" YesNo, default No.
      risk: Medium. UpdateAlways adds CPU on every update interval (trader runs many charts); PC clock skew vs exchange timestamps can confirm a hair early — mitigated by the epsilon and by the fact bar N's OHLC cannot change after its end time; out-of-order late prints from a feed outage could theoretically vi
      verify-notes: Refuted AS SPECIFIED — two implementation-breaking defects, both fixable. (1) WRONG TIME API: the ACSILProgrammingConcepts 'Determining if Last Chart Bar is Closed' method it cites prescribes comparing BaseDateTimeIn + GetBarPeriodParameters period against sc.GetCurrentDateTime(), NOT sc.CurrentSystemDateTime. sc.GetCurrentDateTime() 'returns... the current Date-Time in the time zone of the chart the study is applied to' and during a chart replay returns the replay-calculated time (== sc.CurrentDateTimeForReplay). sc.CurrentSystemDateTime exists but has 'the Time Zone setting in Global Setting

  [P4] Signal-stability telemetry: per-bar flicker counter exposing how often Live-mode signals were ghosts
      priority=4  verdict=CONFIRMED
      mechanism: In TAState add (outside TARecurrence, deliberately NOT rolled back): `int flicker_count; int flicker_last_index; unsigned char flicker_prev_flags;`. Each call: if idx != flicker_last_index reset {count=0, prev_flags=current 4-bit flag pack (full_bull|full_bear<<1|mq_bull<<2|mq_bear<<3), last_index=idx} else { if (pack != prev_flags) count++; prev_flags=pack; }. On the new_bar_commit call store the finished bar's count into SG_PRISM_BEAR.Arrays[1][idx-1] (free slot) and, when the committed bar carried any signal, show an Info Panel row "Signal flicker: N changes intrabar" plus a rolling ghost r
      inputs: None (row appears only when Info Panel is on; could piggyback a Yes/No at index 108 if panel clutter matters).
      risk: Low. Counter is inherently tick-path-dependent: it reads 0 for all bars after a reload/full recalc (document this; it measures live sessions only). No effect on signal logic.
      verify-notes: Verified. Hooks: TAState (TA_State.h:117-134) is the right home — members outside TARecurrence don't roll back; GetPersistentPointer lifecycle verified (ta_get_state/ta_free_state, TA_State.h:144-156, freed in LastCallToFunction line 511). SG_PRISM_BEAR.Arrays[1] confirmed free by grep (no Arrays[] usage at all on SG 23-26) and distinct from P1's Arrays[0]; 12 extra arrays per subgraph confirmed in docs. compute_PRISMSignals call site at TrendArchitect.cpp:846-862 with PRISMResult flags full_bull/full_bear/mq_bull/mq_bear (mod_PRISMSignals.h:54-55) — the 4-bit pack is well-defined. Info Panel 
====================================================================================================
REVIEWER: outcome-logger

  [V1] Gate-state feature stash: extend PRISMResult + persist the fire-time feature vector in unused subgraph extra Arrays
      priority=1  verdict=CONFIRMED
      mechanism: 1) Extend struct PRISMResult (source/modules/mod_PRISMSignals.h:45-56) with fields that are already computed as locals but currently discarded: double fk_norm (normalized fast-KAMA slope, local at ~line 284-288), double bq_ratio (line 184), bool chop_bull/chop_bear (lines 290-293), int sig_path (0=immediate imm_*, 1=Structure-Lock release, 2=Bar-Quality release — derive from imm_bull/bull_released/bq_bull_released at lines 220-243), int elev_mask (bit0..3 = e1..e4 flags, lines 329-353). Assign into `out` where each local is computed; zero behavioral change. 2) In source/TrendArchitect.cpp, nex
      inputs: 
      risk: Near zero: additive struct fields and hidden-array writes only; nothing in the signal path changes. Only care point: keep the new Arrays slot assignments documented in TA_Subgraphs.h so a future module doesn't collide (SG_RAW_KAMA_ALIGN.Arrays[0..1] precedent shows slots are shared by convention).
      verify-notes: All hook points verified in source: PRISMResult at mod_PRISMSignals.h:45-56; bq_ratio local at :184; fk_norm at :284-288; chop_bull/chop_bear at :290-293; imm_*/(bull|bear)_released/bq_*_released for sig_path at :212-243; e1..e4 at :329-353; out={} at :101 zeroes new fields on the idx==0 early return (:111-121). TrendArchitect.cpp raw-output block verified at :881-885 (PRISM call :846, suppress-flag history writes :753-754). Grep confirms SG_RAW_ER(27)/SG_RAW_CCO(28)/SG_RAW_AO_EFF_LEN(30) write only their main [idx] today (no extra Arrays used) and SG_RAW_KAMA_ALIGN uses only Arrays[0..1]; SG_

  [V2] mod_SignalLogger.h: closed-bar confirmed-signal capture with AO-style ATR-tier resolution and one CSV append per resolved signal
      priority=1  verdict=CONFIRMED
      mechanism: NEW module source/modules/mod_SignalLogger.h + 3 opt-in inputs in TA_Inputs.h/SetDefaults: IN_SIGLOG_ENABLE=106 (YesNo, default 0 — baseline parity untouched), IN_SIGLOG_HORIZON=107 (int K bars, default 0 = follow IN_AO_MAX_BARS), IN_SIGLOG_INCLUDE_MQ=108 (YesNo, default 1 — marginal dots are the control group). STATE: add to TAState (TA_State.h:117 — deliberately NOT TARecurrence: the logger only ever reads CLOSED bars, so it must not be wiped by commit/rollback, mirroring the alert_fired_time precedent at TA_State.h:122-126): std::vector<SigLogPending> pending (entry: double fire_time; int d
      inputs: 
      risk: Low-moderate. (1) File I/O in a study function: bounded by open/append/close per resolved signal only (minutes apart on the user's chart speeds) — the docs' own recommended pattern; io_disabled latch prevents error-loop spam. (2) Bars closed while a full recalc is in flight are skipped by the live-o
      verify-notes: APIs verified against the real ACS_Source v2760 headers (downloaded from sierrachart.com/downloads/2760/ACS_Source/): int32_t OpenFile(const SCString& PathAndFileName, const int Mode, int& FileHandle); int32_t WriteFile(const int FileHandle, const char* Buffer, const int BytesToWrite, unsigned int* p_BytesWritten); int32_t CloseFile(const int FileHandle); n_ACSIL::OpenFileModeEnum = exactly {FILE_MODE_CREATE_AND_OPEN_FOR_READ_WRITE=1, FILE_MODE_OPEN_EXISTING_FOR_SEQUENTIAL_READING=2, FILE_MODE_OPEN_TO_APPEND=3, FILE_MODE_OPEN_TO_REWRITE_FROM_START=4}; NO AppendTextToFile/AppendToFile exists an

  [V3] execution/analyze_signals.py: per-gate win rates, direction-aware threshold sweep, markdown report
      priority=2  verdict=CONFIRMED
      mechanism: New stdlib-only script (argparse + csv + statistics, matching the house style of execution/compare_tv_export.py: module docstring with usage, --flags, exit codes). CLI: analyze_signals.py FILE_OR_GLOB... [--win-tier 2 | --win-mfe 1.5 [--max-mae 1.0]] [--min-n 30] [--group symbol,bar_period,class] [--holdout-after DATE] [--out report.md]. STAGE 1 load: glob the TA_signals_*.csv files, validate schema_ver, dedupe on (fire_time_raw, direction, chart), coerce numerics, drop tier==-1 censored rows (counted separately). STAGE 2 baseline: per group (default symbol × bar_period × class) — n, win% (def
      inputs: 
      risk: Analysis risk, not code risk: with few rows the sweep will overfit — mitigated by --min-n guards, UNRELIABLE flags, decile-grid (not exhaustive) cutoffs, and the holdout split; the report must present cutoffs as hypotheses. Tick-chart sessions generate rows slowly (signals are deduplicated flips), s
      verify-notes: No ACSIL surface — pure Python stdlib, so no API risk. House-style reference execution/compare_tv_export.py exists and matches the described style (module docstring with usage, --flags); directives/trendarchitect.md exists for the loop documentation. The cutoff→existing-input mapping holds: IN_PRISM_ER_THRESH(85), IN_PRISM_KAMA_SLOPE_MIN(86), IN_TRG_KAMA_THRESH(71) all exist in TA_Inputs.h, so a validated threshold is genuinely a one-input change. Consumes only the V2 schema; every column listed traces to a V1-stashed or already-persisted feature. Caveats: entirely dependent on V2 landing (inc

  [V4] Intrabar flicker counter: measure live-bar signal repaint per signal and log it as a confidence feature
      priority=3  verdict=PLAUSIBLE
      mechanism: In TAState (outside TARecurrence — per-tick observational state that must NOT roll back): double flicker_bar_time; bool prev_state[4] (full_bull, full_bear, mq_bull, mq_bear); int flip_count[4]. In TrendArchitect.cpp right after the PRISM subgraph writes (lines 876-879), only when idx==sc.ArraySize-1 && !sc.IsFullRecalculation: if sc.BaseDateTimeIn[idx].GetAsDouble() != flicker_bar_time → reset counters and set flicker_bar_time; else for each of the 4 signal states compare against prev_state and increment flip_count on change; store prev_state. At the V2 enqueue for closed bar b (which is the 
      inputs: 
      risk: Minimal: purely observational, no signal-path interaction. Caveat: flicker is only defined for live-observed bars (which is exactly the set V2 logs, so the column is always populated); counters are per-chart-instance and reset on DLL reload mid-bar (one bar of undercounted flicker — negligible).
      verify-notes: API facts fine (BaseDateTimeIn[].GetAsDouble/ArraySize/IsFullRecalculation all already used at TrendArchitect.cpp:967-968); hook point exists (PRISM subgraph writes at :876-879); TAState-outside-TARecurrence placement is correct for per-tick observational state. REFUTED DETAIL / ordering defect as-specified: the flicker block runs at ~:880, BEFORE the V2 logger call after :1012 in the same update pass. On the first tick of a new bar the block sees time!=flicker_bar_time and resets flip_count — so when the logger then enqueues the just-closed bar in that same pass, the counters it reads are alr
